# Required: URL of your Directus instance
DIRECTUS_URL=

# Required: Static token from Directus (Admin -> User Directory -> select user -> generate token)
DIRECTUS_TOKEN=

# Optional: legacy Docker/CI build argument; not used by current application code
PUBLIC_SCRUMLR_SERVER_URL=